{
    "name": "trello-api-sql",
    "version": "1.0.0",
    "description": "",
    "main": "api.js",
    "scripts": {
        "start-prod": "node api.js",
        "start-dev": "nodemon api.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "docker-build-dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml build",
        "docker-run-dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d",
        "docker-build": "docker compose build && docker image push julianmedwards/trello-api-sql:prod"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/julianmedwards/trello-api-sql.git"
    },
    "author": "",
    "license": "ISC",
    "bugs": {
        "url": "https://github.com/julianmedwards/trello-api-sql/issues"
    },
    "homepage": "https://github.com/julianmedwards/trello-api-sql#readme",
    "dependencies": {
        "mysql2": "^3.2.0",
        "restify": "^11.0.0",
        "restify-errors": "^8.0.2",
        "sequelize": "^6.30.0",
        "sequelize-cli": "^6.6.0"
    },
    "devDependencies": {
        "nodemon": "^2.0.22"
    }
}
